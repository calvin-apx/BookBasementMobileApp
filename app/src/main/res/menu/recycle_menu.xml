<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/available_book_id"
            android:icon="@drawable/available_book"
            android:title="Available Books" />
        <item
            android:id="@+id/message_us_id"
            android:icon="@drawable/message_us"
            android:title="Message Us" />
        <item
            android:id="@+id/appointment_id"
            android:icon="@drawable/appointment_recycle"
            android:title="Appointments" />
    </group>
    <item android:title="Options">
        <menu>
            <item
                android:id="@+id/setting_id"
                android:icon="@drawable/settings"
                android:title="Settings" />
            <item
                android:id="@+id/logout_id"
                android:icon="@drawable/logout"
                android:title="Logout" />
        </menu>
    </item>
</menu>